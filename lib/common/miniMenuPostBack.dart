import 'package:kirihare/model/health_room_model.dart';
import 'package:kirihare/model/msg_model.dart';

class MiniMenuPostBack {
  MsgModel addMiniMenu(MsgModel msg, List<PostbackModel> list) {
    list.forEach((element) {});

    return msg;
  }
}
